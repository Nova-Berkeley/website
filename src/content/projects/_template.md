---
# ────────────────────────────────────────────────────────────────
#  CASE STUDY TEMPLATE
# ────────────────────────────────────────────────────────────────
#  Files starting with an underscore are ignored by the site, so this
#  one never publishes. To add a real case study:
#
#    1. Copy this file to a new name, e.g. `acme-pricing.md`
#    2. Fill in the fields below and delete these comments
#    3. Commit — the Projects page picks it up automatically and the
#       "case studies coming soon" state disappears on its own
#
#  ⚠ CLEARANCE: only publish a client name once the engagement
#  contract or NDA is confirmed to permit it. If it does not, leave the
#  project out rather than inventing a descriptor, or get sign-off on
#  the exact wording you will use.
# ────────────────────────────────────────────────────────────────

# Client name, exactly as cleared for publication.
client: ''

# e.g. "Fall 2025"
semester: ''

# One line, past tense, quantified where you can defend the number.
outcome: ''

# ---- Optional ----
# featured: true      # the big case study at the top of the page
# lead: true          # the larger card in the grid below it
# problem: ''         # featured only — the question the work answered
# metric: ''          # featured only — headline figure, e.g. "+18%"
# metricLabel: ''     # featured only — what the figure measures
# anonymized: true    # name is a cleared descriptor, not the real client
# image: ''           # path under /public, e.g. "/projects/acme.jpg"
# order: 1            # lower sorts first
---

Anything written below the dashes becomes the long-form write-up later.
It is not published yet — the structure is just ready for it.
